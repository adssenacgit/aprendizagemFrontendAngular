import { CheckClassPipe } from './check-class.pipe';

describe('CheckClassPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckClassPipe();
    expect(pipe).toBeTruthy();
  });
});
